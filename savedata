

class agility extends React.Component{
	
  constructor(props) {
    super(props);
    this.state = {value: null}; /*set the default state of the class*/	
	}

	render(){
		return(
			<button onClick={() => this.setState({value: "1"})}>
    			{this.state.value} {{/*will be 1 after button is clicked?*/}}
  			</button>);
		/*re-render the class and change the state to 1 (should not be button but when the counter in done)*/
		/*make a counter that changes state value*/
	}
}

class levelagility extends agility{
	constructor(props) {
    super(props);
    this.state = {value: null}; /*set the default state of the class*/	
	}
	render(){
	let yes;
		if(agility.state.value == 10){ 
			yes = "you leveled up";
		}
		return(
			<p> yes </p>);
	}
}

